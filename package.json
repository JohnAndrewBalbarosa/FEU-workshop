{
  "name": "feu-workshop",
  "private": true,
  "workspaces": [
    "portfolio",
    "dashboard"
  ],
  "scripts": {
    "build:portfolio": "npm --workspace portfolio run build",
    "build:dashboard": "npm --workspace dashboard run build",
    "build": "npm run build:portfolio && npm run build:dashboard",
    "dev:portfolio": "npm --workspace portfolio run dev",
    "dev:dashboard": "npm --workspace dashboard run dev",
    "typecheck": "npm --workspace portfolio exec tsc -- -b && npm --workspace dashboard exec tsc -- --noEmit"
  }
}
